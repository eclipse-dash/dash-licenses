# Publishing `dash-licenses-wrapper`

```bash
# Setup npm token. e.g.
npm adduser
<provide user/password>
cd nodejs-wrapper
# step package version in `package.json`
# publish package
npm publish 
```
